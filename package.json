{
  "name": "property-lk",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "seed": "turbo run seed --filter=@property-lk/db",
    "format": "turbo run format"
  },
  "devDependencies": {
    "pnpm": "10.8.1",
    "turbo": "^2.5.0",
    "typescript": "^5.8.2"
  }
}
